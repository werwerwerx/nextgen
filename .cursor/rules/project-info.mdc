---
description: 
globs: 
alwaysApply: false
---
Описание из проекта, связанного с заказом
Требуется разработать веб-приложение — партнёрский лидогенератор с витриной актуальных курсов и удобной админ-панелью для работы с лидами.
Захват контактных данных пользователей через форму с валидацией

Автоматическое отображение списка актуальных курсов из внешнего источника с регулярным обновлением

Перенаправление пользователя по партнёрской ссылке только после успешной записи лида в базу данных

Все остальные подробности при отклике, также обсуждение бюджета и сроков тоже при отклике.

NextGen Appletown — партнёрский лидогенератор для Neural University

📍 Цель:

Создать веб-приложение по адресу https://nextgen.appletownworld.com, которое:

Захватывает контактные данные пользователей

Автоматически отображает информацию о курсах с https://neural-university.ru

Перенаправляет пользователя по партнёрской ссылке только после успешной записи в базу данных

Обеспечивает простую админ-панель для просмотра, экспорта и анализа лидов

🔧 Основные требования:

1. 🌐 Сайт (frontend)

Фреймворк: Next.js

Стилизация: Tailwind CSS

Страницы:

/ — главная с формой и витриной курсов

/thanks — страница благодарности (необязательно, можно через useRouter().push(...))

/go/[slug] — (опционально) промежуточная редирект-страница

/admin — защищённая панель администратора

Функции:

Лендинг с УТП, отзывами, блоком курсов

Форма захвата лида (с валидацией)

Кнопка «Начать обучение» → после POST и успешной записи в БД → редирект на партнёрскую ссылку

Все внешние ссылки должны вести на:

https://neural-university.ru/sales-0624?utm_source=nextgen&amp;utm_campaign=lead&amp;utm_content=course-name

2. 🧾 Форма захвата

Поля:

name — строка, обязательное

email — email, обязательное

phone — строка, опциональное

interest — строка, опциональное

agreement — boolean, обязательное

Поведение:

После submit — POST /api/leads → сохранить данные

После 200 OK → выполнить router.push(partnerLink)

После ошибки → показать сообщение, не выполнять редирект

3. 🗄 Backend + БД

Выбор 1 (рекомендуемый): Supabase (PostgreSQL + Edge Functions)

Выбор 2: Node.js backend (Express) + PostgreSQL

Таблица leads:

Поле Тип Комментарий

id UUID (PK) 

name text 

email text уникальность необязательна

phone text 

interest text 

created_at timestamp по умолчанию now()

ip text по возможности

utm jsonb utm_source, utm_campaign
4. 🔁 Интеграция с Neural (витрина курсов)

Цель:

Автоматически отображать список актуальных курсов/вебинаров/акций с сайта [neural-university.ru]

Реализация:

Фоновый скрипт парсинга (Node.js или Python)

Обновление по CRON (раз в 6–12 часов)

Хранить в Supabase таблицу courses:

Поле Тип Комментарий

id UUID 

title text 

description text 

price text 

start_date date 

source_url text 

created_at timestamp 

Показывать только courses.is_active = true на фронте

5. 🔐 Панель администратора (/admin)

Функции:

Авторизация (basic через env-пароли или Supabase auth)

Просмотр таблицы лидов

Поиск, сортировка, фильтрация

Экспорт CSV

График по created_at (Chart.js или аналог)

6. 📩 Интеграции

Telegram-бот: отправка имени + email + время в канал

Email-уведомление (SendGrid / Supabase SMTP)

GA4 + Meta Pixel + UTM-сбор

7. ⚠ Валидации и защита

Все формы валидировать на клиенте и сервере

HTTPS обязателен

Защита от спама: honeypot или Google reCAPTCHA v3

📦 Результат

Готовый сайт по адресу nextgen.appletownworld.com

Панель администратора для работы с лидами

Автообновляемая витрина курсов с Neural University

Уведомления в Telegram/email

Мы придерживаемся стека Supabase (PostgreSQL + Edge Functions) по следующим причинам:

встроенный auth, REST API и админ-панель

минимальные DevOps-нагрузки

лучшая совместимость с Next.js

возможность масштабирования без переписывания архитектуры

открытая документация и простая поддержка другими разработчиками в будущем

Symfony — отличная система, но её внедрение в текущий проект усложнит поддержку и нарушит модульную логику. Поэтому просим придерживаться Supabase.

Домен зарегистрирован на Hostinger.

Поддомен nextgen.appletownworld.com создан. Мы либо внесём DNS-записи вручную по вашей инструкции (CNAME/Vercel), либо предоставим доступ. Установка SSL не потребуется — он подключается автоматически через Vercel.

Да, UTM-метки нужно сохранять вместе с заявкой в поле utm (JSON), включая utm_source,
